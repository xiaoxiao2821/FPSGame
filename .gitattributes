# Auto detect text files and perform LF normalization
* text=auto

# UE / binary assets — keep as binary, never normalize line endings (corruption risk)
*.uasset binary
*.umap binary
*.png binary
*.map binary
*.zip binary
*.mp4 binary
