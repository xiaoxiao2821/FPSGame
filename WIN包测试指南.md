# WIN 包测试指南(TDM 团竞模式)

> **适用对象**: 拿到 `FPSGame` 仓库 + win 包后,在 Windows 上进行功能测试。
> **配套文档**: `TDM_需求文档.md`(效果基准)、`TDM_开发对话记录.md`(开发过程)。
> **前提**: Windows 系统,无需安装 UE;联机测试需同一局域网(或端口转发)。

---

## 1. 包的位置与结构

win 包是**编辑器打包产物**(Game 目标,引擎模块预编译,数分钟出包):

```
Saved/StagedBuilds/Windows/
├── FPS.exe                  ← 主程序(约 171KB 引导器, 实际内容在 Pak)
├── Engine/                  ← 引擎运行库
├── FPS/                     ← 游戏内容
│   ├── Content/Paks/
│   │   ├── FPS-Windows.pak / .ucas / .utoc   ← 打包的关卡+资产
│   │   └── global.ucas / .utoc               ← IoStore 全局索引
│   └── Saved/Logs/FPS.log   ← 游戏运行日志(按实例递增 FPS_1/2/3.log)
└── Manifest_*.txt           ← 文件清单(诊断用)
```

**地图配置**(打包时已固化):
- 默认地图 = `Lvl_Login`(登录场景,启动先进这里)
- ServerDefaultMap = `Lvl_Shooter`(服务器/主机加载的对战图)

---

## 2. 三种启动方式

### 方式 A:单机快速测试(Listen Server 主机自玩)
打开 cmd,进入包目录后执行:

```bat
cd /d D:\UnrealProject\FPS\Saved\StagedBuilds\Windows
FPS.exe "/Game/Variant_Shooter/Lvl_Shooter.Lvl_Shooter?listen" -log
```

- 直接进 `Lvl_Shooter` 开主机,自己就是玩家(同时也是服务器);
- 想测试完整登录流程,直接双击 `FPS.exe` 会进登录场景(见方式 C);
- `-log` 会在窗口里显示日志,测试建议必加。

### 方式 B:局域网联机(一台主机 + 多客户端)
**主机**(开房间):
```bat
cd /d D:\UnrealProject\FPS\Saved\StagedBuilds\Windows
FPS.exe "/Game/Variant_Shooter/Lvl_Shooter.Lvl_Shooter?listen" -log
```

**客户端**(加入):
```bat
cd /d D:\UnrealProject\FPS\Saved\StagedBuilds\Windows
FPS.exe 主机IP:7777 -log
```
- 同一台机器测试填 `127.0.0.1:7777`;
- 默认端口 7777;**首次联机需在防火墙放行 7777 UDP**(Windows 会弹窗,点允许);
- 全部玩家进入后,任一人点"开始游戏"进入准备阶段 → 对战。

### 方式 C:完整登录流程(推荐验收)
1. 主机和客户端都**双击 `FPS.exe`**(进入登录场景 `Lvl_Login`);
2. 主机先开房间:按 `~` 打开控制台,输入
   ```
   open /Game/Variant_Shooter/Lvl_Shooter.Lvl_Shooter?listen
   ```
3. 客户端在登录菜单地址框填 `主机IP:7777`(本机填 `127.0.0.1:7777`),点**开始游戏** → 连接进入。

> 注意:登录菜单的"开始游戏"是连接动作,需要目标服务器已在运行;单独一人直接点开始连 `127.0.0.1` 而本机没开服务器会失败,属预期。

---

## 3. 完整回归测试清单

按此清单逐项验收,每一项都对应 `TDM_需求文档.md` 的章节:

### 3.1 登录场景(§2.1)
- [ ] 启动进 `Lvl_Login`,**客户端显示登录菜单**(标题 + 地址/端口输入框 + 开始按钮)
- [ ] 菜单出现时鼠标可动、为 UI 模式
- [ ] 填写 `127.0.0.1:7777` 点开始 → 成功进入主游戏场景

### 3.2 主菜单与选队(§2.2/§3)
- [ ] 进入后显示 TDM 主菜单(标题 + 开始游戏按钮)
- [ ] 有**选队**按钮:红队/蓝队,点击后本局固定该队
- [ ] 不选队时由服务器轮转分配(先进先红后蓝交替)
- [ ] 未点开始前,输入被冻结(不能移动/射击)

### 3.3 准备阶段(§2.3/§4/§5)
- [ ] 有人点"开始游戏"后,全员显示 **30 秒倒计时**
- [ ] 准备期间**不能造成伤害**(互相打不掉血)
- [ ] 准备期间**不生成 AI**
- [ ] 倒计时结束自动开始,比分归零

### 3.4 对战核心(§3/§5/§6/§7)
- [ ] 出生/重生带 **3 秒无敌**(需求,非 bug;期间受击不掉血)
- [ ] **友军伤害免疫**(打队友不掉血)
- [ ] 默认武器手枪,开火有子弹数变化;打空后自动**换弹**(手枪 0.5s / 步枪 0.8s / 爆弹 1.0s),换弹期间不能开枪
- [ ] 子弹从枪口射出、打哪指哪;高速子弹不穿墙穿人
- [ ] 爆弹枪手雷为抛物线(设计),落点对准准星
- [ ] 血条/子弹 UI 实时刷新(100 HP,约 4 枪击杀)
- [ ] **AI 行为**: 开局每队补满 4 人(人类+AI);AI 会索敌、追击、持续射击、约 30% 命中率;AI 死亡 2s 后复活;人类进场顶替随机一个 AI
- [ ] 红蓝**队伍染色**正确(所有人按队着色,客户端可见)

### 3.5 死亡/复活(§3.2)
- [ ] 死亡后 2s 回到**本方出生点**复活,满血 + 重新无敌
- [ ] 复活后视角切回第一人称、能正常移动射击

### 3.6 胜负与结算(§3.3)
- [ ] 某队先到 **30 击杀** → 全员显示结算屏(胜方 + 比分)
- [ ] 结算屏点"返回菜单" → 回到主菜单,可再开下一局

### 3.7 联网表现(§8)
- [ ] 多客户端看到的子弹/死亡/复活/比分一致(服务器权威)
- [ ] 客户端断线/掉线不崩溃,主机退出所有人回登录

---

## 4. 日志与诊断

**日志位置**:
```
Saved\StagedBuilds\Windows\FPS\Saved\Logs\FPS.log     (第一个实例)
Saved\StagedBuilds\Windows\FPS\Saved\Logs\FPS_2.log   (第二个实例, 依此类推)
```

**关键日志前缀**(按前缀 grep 可定位问题):

| 前缀 | 含义 |
|---|---|
| `LOGIN:` | 登录场景流程(登录 UI 创建/开始游戏) |
| `TDM_FLOW` | 对局状态机(出生/准备/开始/结算/重生) |
| `PROJ_HIT` / `PROJ_DMG` | 子弹命中 / 伤害结算(服务器) |
| `AMMO_UI` / `HP_UI` | 弹药/血条 UI 事件链 |
| `SpawnBot diag` | AI 生成诊断(controller 是否有效) |
| `FindTeamPlayerStart` | 出生点选择(team/slot/候选数) |

**控制台常用命令**(按 `~` 打开):
```
open <地图>?listen         # 开房间
open <IP>:7777             # 加入
stat net                   # 网络统计(验证复制量/延迟)
stat fps                   # 帧率
quit                       # 退出
```

---

## 5. 常见问题排查

| 现象 | 原因 | 处理 |
|---|---|---|
| 开局打不掉血 | 出生无敌 3 秒(需求) | 等 3 秒再打;日志见 `SPAWN PROTECT` |
| 打队友不掉血 | 友伤免疫(需求) | 正常,不是 bug |
| 倒计时不出现 | 没人点"开始游戏" | 需至少 1 名玩家点主菜单开始按钮 |
| AI 站着不动 | 已修复(方案 B) | 检查 `SpawnBot diag` 是否 controller=VALID;日志看 NavMesh tiles |
| 连接失败 | 服务器没开/端口不通 | 先开主机再连接;防火墙放行 7777 UDP |
| 双开闪退/卡 D3D12 | 远程桌面/无桌面会话渲染问题 | 本机桌面直接运行;或加 `-nullrhi -unattended`(仅无头测试) |
| 窗口无日志输出 | 没加 `-log` | 命令行加 `-log` 启动 |
| 想直接测对战跳过登录 | — | 用方式 A 的命令行带 map?listen 直接进 |

---

## 6. 与开发环境的区别(仅提示,测试用不到)

- 本 win 包是 **Game 目标**(`UE_GAME=1`),**不能以 `-server` 参数启动为专用服务器**(编译期决定的硬限制,详见 `TDM_开发对话记录.md` §3.4);
- 需要**独立 DS 服务器**的部署场景,开发期用 `UnrealEditor.exe FPS.uproject -server -log`,正式部署需源码版引擎打 Server 包;
- win 包测试用**方式 A/B(C)** 的 Listen Server 模式即可覆盖全部联机玩法。
