# 2026-08-16

## TDM UI 接线 — 用户决定手动做 (00:33)
- **背景**: 无头脚本尝试接线 BP_TDMMainMenu(Button_Start → ServerStartMatch)多次受阻: ① GetGameMode 是纯节点(无 exec)接线顺序错; ② 后续无头编辑器启动卡 ZenServer("Waiting for ZenServer to be ready...", 需要 `-NoZenAutoLaunch` 参数); ③ WidgetTree 无头下 protected 读不了 → 结算屏 Button_Back 的 OnClicked 事件无法脚本创建。
- **用户决定**: 自己接线, 只给接口。资产已验证未损坏(md5 与 backup_tdm_20260816 一致); 残留 UnrealEditor/zenserver 已清理。
- **资产现状**: BP_TDMMainMenu 有 Button_Start + Button_End(已建 OnClicked 事件, 未接线); BP_TDMEndScreen 有 Button_Back(未建 OnClicked 事件) + ResultText/ScoreText(命名正确, Populate 自动填)。
- **接口要点(用户接线用)**:
  - 主菜单 Button_Start → **Get Player Controller** → **Cast to ShooterPlayerController** → **ServerStartMatch**(Server RPC, PC 上)
  - 结算 Button_Back → 先 Designer 建 OnClicked → Get Player Controller → Cast to ShooterPlayerController → **ServerReturnToMainMenu**
  - ⚠️ **客户端没有 GameMode**(GetAuthGameMode 仅服务器有值), RPC 必须放 PlayerController 上(已修正, 见下节)
- **无头经验追加**: 无头编辑器挂 ZenServer 时加 `-NoZenAutoLaunch`; GetGameMode 是纯节点。

## 用户发现关键架构错误: 客户端没有 GameMode (01:03)
- **用户质疑**: "客户端哪来的 gamemode?" — 完全正确: `Get Game Mode` 底层 `World->GetAuthGameMode()` 只在服务器有值, 纯客户端为 null → 旧接线 (按钮 → GetGameMode → Cast → ServerStartMatch on GameMode) 在真 DS 上必炸 (listen PIE 能跑是因为本地玩家就在服务器进程, 掩盖了问题)。
- **修正 (待编译)**: Server RPC 从 `AShooterTDMGameMode` 挪到 `AShooterPlayerController`:
  - 新增 `UFUNCTION(Server, Reliable, BlueprintCallable) AShooterPlayerController::ServerStartMatch()` / `ServerReturnToMainMenu()`, 实现里 `Cast<AShooterTDMGameMode>(GetWorld()->GetAuthGameMode())->StartMatch()/ReturnToMainMenu()`。
  - GameMode 上删除这两个 RPC, 保留 StartMatch/ReturnToMainMenu (服务器专用, 注释已标明)。
- **最终接线 (用户操作)**: 按钮 OnClicked → **Get Player Controller** → **Cast to ShooterPlayerController** (或 BP_ShooterPlayerController) → **ServerStartMatch** / **ServerReturnToMainMenu**。
- **待办**: 用户关编辑器 → 编译 → 重开 → 接线 → PIE/DS 验证。

## 修复双击/镜头固定 + 简化流程 (01:23, 用户手动编译)
- **用户反馈**: ①开始需点两次才进入; ②开始后镜头不跟随 pawn(固定), pawn 可操作; ③代码复杂, 要求 RPC 直接调用、尽量简洁。
- **根因**:
  - 双击: Client_ShowMainMenu 设 UIOnly 但没 `SetWidgetToFocus`/`SetUserFocus` → 新菜单第一次点击被焦点获取吃掉。
  - 镜头: 客户端 RPC 里本地 SetIgnoreMoveInput/LookInput 与服务器复制标志冲突 + 开始比赛后未显式 `SetViewTarget(pawn)`。
- **已改 (用户手动编译, 未代编译)**:
  - Client_ShowMainMenu / Client_ShowEndScreen: 加 `SetUserFocus` + `FInputModeUIOnly.SetWidgetToFocus`; **删除客户端本地 SetIgnoreMoveInput/LookInput**(冻结完全由服务器 PostLogin/StartMatch 复制标志负责)。
  - Client_HideMainMenu → 重命名 **Client_OnMatchStarted**: 藏菜单 + 建 HUD + GameOnly + cursor off + **SetViewTarget(GetPawn())** 重绑视角。
  - GameMode StartMatch 调用点同步改名。
  - 头文件含 `TSharedPtr<SWidget>` 需 `#include "Widgets/SWidget.h"`(已加)。
- **RPC 结构不变(DS 正确)**: Server RPC 在 PC (ServerStartMatch/ServerReturnToMainMenu), Client RPC 在 PC (ShowMainMenu/OnMatchStarted/ShowEndScreen), GameMode 只做服务器逻辑。

## 编译收尾 (01:35) — 用户要求由我编译
- **修复**: `Client_OnMatchStarted` 里局部变量 `Pawn` 隐藏 `APlayerController::Pawn` 成员 (C4458) → 改名 `ViewPawn`。
- **编译过程 (UBA 幻影反复)**: 手动编译全部变更文件 + 手动 link.exe DLL rsp + 手动 link.exe /LIB .lib rsp → 最终 **DLL 生成成功 (01:35, 680960 字节, PE 合法, 含新符号)**。构建报告残留的 "Link UnrealEditor-FPS.lib" 失败是 UBA 幻影(import 库, 单模块项目运行时不需要)。
- **验证**: Binaries/Win64/UnrealEditor-FPS.dll 时间戳 01:35, python 提取字符串确认含新代码(Client_OnMatchStarted/ServerStartMatch 等)。
- **待用户**: 打开编辑器 → PIE 验证: 单击开始、镜头跟随 pawn、结算/返回。

## TDM 三大修复: 个人进场 + AI 补人 + 计分广播 (02:00)
- **问题**: ① 所有视口都点开始才解冻(StartMatch 是全局的); ② 无 AI 补人(依赖关卡摆 Spawner, 实际没有); ③ 计分不刷新(ReportKill 里 ShooterUI 是服务器端、BeginPlay 已清空, BP_UpdateScore 永不执行)。
- **改动**:
  - PC: 新增 `bMatchReady` + `SetMatchReady/IsMatchReady`; `ServerStartMatch` 改为调 `TDM->StartPlayerMatch(this)`(只让点击者进场); `OnPossess` 按 Ready 分支(未 Ready→弹菜单, Ready+Playing→配枪, Ended→不弹); 新增 Client RPC `Client_UpdateScore(Team, Score)` 推比分到客户端 HUD。
  - GameMode: 新增 `StartPlayerMatch(PC)`(标记Ready→解冻该玩家→Client_OnMatchStarted→发武器; 首个进场者置 Playing + EnsureBotRoster + BP_OnMatchStarted); `PostLogin` 一律冻结未 Ready 玩家(不再只看 MainMenu 阶段); `ReportKill` 改为遍历所有 PC 发 `Client_UpdateScore`(删掉失效的服务器 ShooterUI 分支); **AI 补位内置进 GameMode**(BotClass 默认 `BP_ShooterNPC`, 构造函数 FClassFinder 硬编码路径 `/Game/Variant_Shooter/Blueprints/AI/BP_ShooterNPC.BP_ShooterNPC_C`; EnsureBotRoster/SpawnBot/OnBotDied 补位, 人类数实时计入, 不收缩已有 bot——超员随 bot 死亡自然收敛)。
- **关键约束**: 关卡里**不要**再放 AShooterTDMSpawner(会和 GameMode 内置补位双倍生成 bot)。AShooterTDMSpawner 类保留未删。
- **构建**: 又遇 UBA 幻影失败(编译/链接报错但手动全过)——手动编 7 文件 + 手动 link DLL 绕开, DLL 已验证含新符号(01:56, 692KB)。

## DS 权威化改造: 伤害/死亡/重生全部服务器权威 (02:25)
- **根因**: 角色/武器/投射物都不复制, 开火客户端本地 SpawnActor 投射物 → 本地 TakeDamage → 本地 Die → 本地重生, 服务器完全不知情; AI 死亡只在击杀者客户端, 不触发服务器补位, 其他客户端看不到。
- **改动**:
  - `FPSCharacter` 构造: `SetReplicates(true)` + `SetReplicateMovement(true)`(⚠️ bReplicateMovement 是 AActor private, 必须用方法)。
  - `ShooterWeapon`: `SetReplicates(true)`; `StartFiring/StopFiring` 客户端→`ServerStartFiring/ServerStopFiring`(Server RPC), 服务器 `StartFiringInternal` 权威连射; `Fire()` 服务器专属(投射物服务器生成); 表现(montage/recoil/弹药HUD)移到 `Multicast_OnFire`(NetMulticast); ServerStartFiring 加死亡防护(Cast<AShooterCharacter> IsDead)。
  - `ShooterProjectile`: `SetReplicates(true)+SetReplicateMovement(true)`(客户端看服务器模拟飞行, NotifyHit 只服务器触发); `BP_OnProjectileHit` 改走 `Multicast_OnHit`(命中特效全端同步)。
  - `ShooterCharacter`: `TakeDamage` 开头 `if (!HasAuthority()) return 0`; `Die`(服务器)保留逻辑(ReportKill/计时重生), 表现(DisableInput/HUD清零/BP_OnDeath)移 `Multicast_OnDeath`; `CurrentHP`/`CurrentWeapon` 加 `UPROPERTY(ReplicatedUsing=OnRep_...)`(⚠️ **ReplicatedUsing 属性必须同时 EditAnywhere/BlueprintReadOnly 暴露, 否则 UHT 崩溃 "aggregate exceptions"**); `DoSwitchWeapon` 改 `ServerSwitchWeapon`+`SwitchWeaponNext`(服务器切换)。
  - `ShooterNPC`: `TakeDamage` HasAuthority 防护; `Die` 服务器保留(OnPawnDeath→补位), ragdoll 表现移 `Multicast_OnDeath`; `BeginPlay` 加 `if (!HasAuthority()) return`(防客户端重复生成武器)。
  - `ShooterPlayerController::OnPawnDestroyed`: 加 `if (!HasAuthority()) return`(防客户端重生, 重生全由服务器 SpawnActor+Possess)。
- **构建**: 头文件加了新 UFUNCTION 后**UHT 不自动跑**(UBA 环境吞 UHT), 需删 `Intermediate/Build/Win64/UnrealEditor/Inc/FPS/UHT` 强制 UHT 重生成; 本次 UHT 崩溃真因 = CurrentWeapon 的 ReplicatedUsing 未暴露。修后 `BUILD_EXIT_CODE=0` 全过(02:21, 708KB, 26 个新符号验证)。
- **已知限制**: ① bot 动画不复制(客户端 bot 姿势可能滑行, 死亡 ragdoll 已同步); ② 客户端开火瞄准点用服务器端角色朝向(有 RTT/2 偏差, 权威判定不受影响)。

## 射击方向俯仰修复 + 第一人称武器跟随镜头 (10:35)
- **问题**: DS 权威化后开火方向由服务器算(服务器 ControlRotation 只有 yaw, 角色复制不同步 pitch) → 射击只剩水平方向; 第一人称手臂/武器 mesh 也不随俯仰转。
- **改动**:
  - `ShooterWeapon`: `StartFiring(const FVector& AimTarget = ZeroVector)`; `ServerStartFiring(FVector AimTarget)`(Server RPC 带瞄准点); 服务器存 `PendingAimTarget`, `Fire()` 优先用它(空则回退 `GetWeaponTargetLocation()` 供 AI bot)。
  - `ShooterCharacter::DoStartFiring`: 客户端传 `GetWeaponTargetLocation()`(camera 射线终点, 含正确 pitch) → 服务器投射物方向全对。
  - `AFPSCharacter`: `Tick` 里 `IsLocallyControlled()` 时 `FirstPersonMesh->SetRelativeRotation(FRotator(ControlRotation.Pitch, 0, 0))`——手臂+武器 FP mesh 随镜头俯仰(相机 bUsePawnControlRotation 不受影响, 因为相机世界旋转被 ControlRotation 覆盖)。
- **要点**: 相机 attach 在 FirstPersonMesh 上但 bUsePawnControlRotation=true, mesh 转 pitch 不会造成双倍俯仰(相机最终 = ControlRotation); NPC 服务器 `StartFiring()` 无参默认兼容。
- **构建**: 又是 UBA 幻影, 手动编 9 文件 + 手动 link, DLL 10:35 (710KB), ServerStartFiring(FVector) 符号验证。

## 手枪伤害修复 + 队伍着色 + AI 顶替策略 (10:53)
- **手枪没伤害根因**: 开局 FillTeamRoster 生成的 bot 全带 3 秒无敌(GrantSpawnProtection), 玩家进场立刻打不动 → **bot 不再有无敌**(NPC BeginPlay 与 SpawnBot 都去掉), 玩家保留 3s 无敌(防 bot 蹲出生点)。
- **AI 顶替策略**: `EnsureBotRoster` → `FillTeamRoster`(开局两队各补满 TeamSize 个 bot, 忽略人类); 玩家 `StartPlayerMatch` 进场后 `ReplaceBotWithPlayer(PC)` → `RemoveRandomBot(Team)` 随机 Destroy 该队一个存活 bot 让玩家顶替; `OnBotDied` 补位公式不变(TeamSize - 人类 - 存活bot)。PostLogin 不再补位(玩家进场才动 roster)。
- **队伍着色**: `AFPSCharacter::ApplyTeamColor(Team)`——对 GetMesh() 每个材质槽 CreateAndSetMaterialInstanceDynamic, 设 "Paint Tint" + "LogoTint" 参数(已确认 MI_Manny_01_New 有 Paint Tint, 02_New 有 LogoTint), RED=(0.85,0.15,0.15) BLUE=(0.15,0.25,0.9); 玩家/NPC 的 SetTeam 里调用(生成/重生/OnPossess 都会走 SetTeam → 自动着色)。第一人称手臂不染(自己视角无必要)。
- **队伍分配**: 保持 PostLogin round-robin(NextTeamSlot%2), 现在有颜色可见了。
- **构建**: UBA 幻影 ×N, 手动编 9 文件 + 手动 link, DLL 10:53 (712KB), 新符号验证。

## 出生无敌恢复 + 需求文档 (10:59)
- **用户裁决**: 出生无敌是需求(玩家+AI 都要), 之前的"bot 无无敌"改动撤销——`NPC::BeginPlay` 和 `SpawnBot` 恢复 `GrantSpawnProtection(SpawnProtectionTime)`。开局前 3 秒打不动是正常现象, 不是 bug。
- **需求文档**: 新建 `D:/UnrealProject/FPS/TDM_需求文档.md`(v1.0)——TDM 模式唯一效果基准: 对局流程/队伍/AI 顶替/出生无敌/武器/计分/DS 架构/UI/配置/已知限制/冲突处理原则。
- **约定(已记入项目记忆 MEMORY.md)**: 后续以文档效果为准; 实现与文档冲突时先确认再改, 不擅自变更; 需求变更需更新文档并升版本。

## v1.1 需求落地 + 染色修复 (11:29)
- **染色 bug 根因修复**: ApplyTeamColor 原来只在服务器执行, 材质实例不复制 → 客户端看不到。改为 TeamByte 加 `ReplicatedUsing=OnRep_Team`(角色/NPC), OnRep 里**各客户端本地** ApplyTeamColor; PC 的 TeamByte 也复制(OnRep_TeamByte)供 HUD 判断己方。
- **友军伤害免疫**: 角色/NPC 的 TakeDamage 开头加同队攻击免疫(EventInstigator 队伍==自己 → return 0)。
- **复活复用 pawn(需求)**: Die 不再 Destroy; RespawnTime 5→2s; 服务器 `Revive()` 复用原 pawn——回出生点(FindTeamPlayerStart)、HP 回满、恢复移动/碰撞、重装武器、重新无敌、`Multicast_OnRevive`(EnableInput+HUD 刷新)。OnRespawn 保留为兼容旧路径。
- **出生点 1-4 红 / 5-8 蓝(需求)**: GameMode 新增 `FindTeamPlayerStart(Team)`——解析 PlayerStart 名字尾部数字(Player1-8), 1-4→RED、5-8→BLUE; 回退 TeamSpawnTags 标签。ChoosePlayerStart/SpawnBot/Revive 统一走它。
- **比分栏左己右敌(需求)**: PC::TeamByte 复制, 客户端 UI 蓝图用 `GetTeam()`(0=RED/1=BLUE)决定左右布局。UI 翻转本身在蓝图做。
- **文档**: TDM_需求文档.md 升 v1.1(友伤/复活/出生点/比分栏/染色说明)。
- **构建**: UHT 又因 ReplicatedUsing 未暴露崩溃(PC TeamByte, 已修); UBA 幻影 ×N, 手动编 13 文件 + 手动 link, DLL 11:29 (722KB), 13 新符号验证。

## 出生点顺序分配 + 染色触发修复 (11:43)
- **出生点顺序分配**: `FindTeamPlayerStart` 改为 round-robin——按名字数字排序(Player1<2<3<4 / 5<6<7<8), 每队用 `NextPlayerStartIndex[2]` 游标顺序下发(slot 递增循环), 不堆积。玩家出生/复活/AI 生成/补位统一走它。
- **染色触发修复(重要经验)**: `ReplicatedUsing` 的 OnRep **只在属性值变化时触发**, 首次复制若值==CDO 默认则**不触发**。角色 TeamByte 默认 0、NPC 默认 1 → RED 玩家(0)/RED AI(0)/BLUE AI(1) 全都不触发 → 只有 BLUE 玩家(1≠0)染色成功。**修复: TeamByte 默认改 255(未分配)**, SetTeam 后 0/1 必 ≠ 默认 → 所有单位首次复制必触发 OnRep_Team 本地染色。
- **文档**: TDM_需求文档.md 升 v1.2(出生点顺序分配 + 染色修复)。
- **构建**: UBA 幻影 ×N, 手动编 13 文件 + 手动 link, DLL 11:43 (725KB)。

## 射击/连射/互伤/复活 5 项修复 (12:06)
- **① 子弹不从枪口射出+偏移**: `CalculateProjectileSpawnTransform` 原来用服务器端 FirstPersonMesh 的 MuzzleSocket(服务器骨骼变换不可靠/socket 错位) → 改用 `PawnOwner->GetActorEyesViewPoint()` 眼睛位置(服务器可靠), 方向=眼睛→客户端瞄准点。子弹从视角中心射出, 打哪指哪。
- **② 连射不跟随准星**: 新增 `ServerUpdateAim`(Server, Unreliable) RPC + 武器 `Tick`: 客户端按住时每 50ms 上报最新 GetWeaponTargetLocation(), 服务器更新 PendingAimTarget, Fire 用最新值。
- **③ 长按失效**: `StartFiringInternal` 冷却中分支改为**无条件 SetTimer(Fire, TimeSinceLastShot)**(原只对 bFullAuto)→ 半自动快速重按(间隔<冷却)也会补发, 不再被吞。
- **④ 玩家互伤无伤害**: 同①(子弹起点错乱打不中), 眼睛起点修复后解决。
- **⑤ 2号视口玩家无法复活**: 死亡表现(BP_OnDeath 蓝图)若销毁 pawn → Revive timer 随 pawn 销毁失效 → 只能走 `PC::OnPawnDestroyed` 重生, 但 `CharacterClass`(EditAnywhere)未配置为空 → SpawnActor 失败。修复: **PC 构造函数默认加载 BP_ShooterCharacter** + OnPawnDestroyed 空类时兜底 `TDM->DefaultPawnClass` 双保险。
- **构建**: UHT 正常(ServerUpdateAim 生成), UBA 幻影 ×N, 手动编 13 文件 + 手动 link, DLL 12:06 (730KB)。
- **注意**: 手枪是半自动(bFullAuto=false), 按住只射一发; 若要自动连射需在 BP_ShooterWeapon_Pistol 勾 bFullAuto(资产配置)。

## 枪口起点 + 伤害排查加固 + 投射物对象池 (12:49)
- **① 子弹从枪口射出**: 客户端 FP mesh socket 是准的(本地动画), 新增 `GetMuzzleWorldLocation()`; `ServerStartFiring/ServerUpdateAim` 增加 muzzle 参数(客户端上报), 服务器存 `PendingMuzzleLocation`, Fire 用它做发射起点; AI/NPC 无 muzzle 时回退眼睛位置。
- **② 玩家互伤排查(全面加固)**: ①投射物 BeginPlay(服务器)强制碰撞: QueryAndPhysics + WorldDynamic + 全 Block + 对 Pawn Block(防蓝图覆盖 profile 导致服务器 sweep 检测不到玩家); ②玩家角色 BeginPlay(所有端)强制 capsule 对 Visibility/WorldDynamic/Pawn = Block(客户端瞄准 trace 能看到玩家、服务器投射物能撞上); ③ProcessHit/NotifyHit 加 `PROJ_HIT`/`PROJ_DMG` 日志(服务器)——测试时输出日志即可定位"子弹是否生成/是否命中/是否扣血"。⚠️ 若日志显示命中但玩家不掉血 → 只剩出生无敌(3s)或友伤误判(TeamByte 未设=255)两种可能。
- **③ 对象池**: GameMode 加 `AcquireProjectile/ReturnProjectile` + `ProjectilePool`(上限 64); 投射物 `ReturnToPool/DeactivateToPool/ActivateFromPool`; 命中/超时**回池不销毁**(DeactivateToPool: 停模拟/隐藏/禁碰撞/重置 bHit), 武器 FireProjectile 从池获取——消除每发 SpawnActor/Destroy 的 GC 卡顿。⚠️ 坑: 池方法参数名 Owner/Instigator 遮蔽 AActor 成员(C4458 当错误), 改 NewOwner/NewInstigator; python 批量替换只改签名没改函数体调用 → 漏改导致 C2248。
- **构建**: UBA 幻影严重(手动编译后重跑还会报已编译文件, 且 UBA 会覆盖/删除手动 obj → 链接 LNK2019), 最终手动重编 GameMode + 手动链接成功, DLL 12:49 (737KB), 池/枪口符号验证。

## 关卡出生点命名不符 → 丢pawn/同点复活/射击错位 连锁修复 (13:00)
- **查证**: `Lvl_Shooter`(WorldPartition, __ExternalActors__ 扫描 8 个 PlayerStart)实际命名 = `Player0` + `PlayerStart2/3/4/5/6/7/8`(无 Player1, 且无 RED/BLUE tag)。
- **连锁根因**: ① FindTeamPlayerStart 按"1-4红/5-8蓝"解析 → Player0 排除 + tag 回退空 → 复活原地(死亡点, 观感=红蓝同点复活); ② OnPawnDestroyed 重生依赖 RED/BLUE tag(关卡没有) → 重生失败 → 2号丢 pawn + 1号看不见 2号; ③ pawn 状态混乱 → 射击起点/方向错乱。
- **修复**: ① FindTeamPlayerStart 兼容 `Num 0-4 = RED / 5-8 = BLUE`(适配关卡实际命名, 规范 Player1-8 为可选建议, 已写入文档 §3); ② PC::OnPawnDestroyed 重生改用 `TDM->FindTeamPlayerStart(TeamByte)`(tag 仅作回退)——pawn 被销毁必能重生; ③ 武器 Fire 起点加**距离保护**: 客户端上报 muzzle 距角色 >500cm 视为异常回退眼睛位置(防"远离角色错位射击")。
- **文档**: TDM_需求文档.md 出生点小节更新(实际命名 0-4/5-8 + 建议规范命名 Player1-8)。
- **构建**: UBA 幻影, 手动编 4 文件 + 手动 link, DLL 13:00 (738KB)。

## 池化彻底重置 + 复活链路日志化 (13:12)
- **池化重置(用户指出的方向残留根因)**: ActivateFromPool 原来只设 Velocity, 但 ProjectileMovement 内部状态(ResetMoveState 未调)、旧 instigator 的 IgnoreActorWhenMoving 未取消 → 复用子弹带上一发影响。修复: SetUpdatedComponent + ResetMoveState() + 重设 Velocity + UpdateComponentVelocity() + DeactivateToPool 取消旧 instigator 忽略(新增 LastInstigator 成员)。
- **复活链路加固**: Revive/Multicast_OnRevive 加日志; Multicast_OnRevive 恢复 SetActorHiddenInGame(false)/mesh visible/capsule collision(防 BP_OnDeath 隐藏后复活不可见); OnPawnDestroyed 重生加成败日志(spawn point / class / SpawnActor null / no class / no point 五类)。
- **下一步(待用户测试)**: 跑一次 PIE, 贴 TDM_FLOW + PROJ_HIT/PROJ_DMG 日志 → 若 "Revive" 有输出=复用路径通; "respawn FAILED" 提示具体断点; PROJ_HIT 有但 PROJ_DMG 无=伤害调用断; 两者都无=子弹没生成/没命中。
- **构建**: UBA 幻影, 手动编 4 文件 + 手动 link, DLL 13:12 (740KB)。

## 代码梳理文档 (13:20)
- 新建 `D:/UnrealProject/FPS/TDM_代码梳理.md`(v1.0): 按类组织 7 个核心文件(AFPSCharacter/AShooterCharacter/AShooterNPC/AShooterPlayerController/AShooterTDMGameMode/AShooterWeapon/AShooterProjectile)的全部主要函数与职责 + 网络设计总览图 + 关键设计决策/踩坑 10 条。

## 网络复制表现加固(能造成伤害但看到的不对) (14:25)
- **检查结论**(三类的复制设置核对): 角色 SetReplicates+SetReplicateMovement ✓; 武器 SetReplicates ✓(无 SetReplicateMovement); 投射物 SetReplicates+SetReplicateMovement ✓(NetUpdateFrequency 默认 100)。
- **加固**: ①武器显式 SetReplicateMovement(false)(transform 靠 actor 级 attach 复制, 防误复制自身 transform); ②武器加 bMeshesAttached + Tick 重试 AttachWeaponMeshes——组件级 mesh attach 不复制, 客户端 BeginPlay 时角色 mesh 未就绪会 attach 失败 → 枪飘错误位置(加固修复); ③投射物 SetNetUpdateFrequency(200) 平滑飞行; ④角色 GetMesh()->VisibilityBasedAnimTickOption = AlwaysTickPoseAndRefreshBones(防蓝图优化导致远离时动画停更/姿态跳变)。
- **构建**: 本次一次通过(BUILD_EXIT_CODE=0), DLL 更新。

## 池化显示时序 + 复活动画恢复 (14:31)
- **子弹"从原地提升到 pawn 高度落下"**: 对象池复用子弹时, 服务器 SetActorTransform(枪口新位置)与 SetActorHiddenInGame(false) 同时复制, 但位置更新有网络延迟 → 客户端短暂渲染在**旧位置(上次回池处/地面)** 再跳到枪口。修复: `ActivateFromPool` 保持隐藏 + `RevealTimer` 延迟 0.03s 后 `RevealFromPool()` 显示——让新位置先复制到客户端。回池时清 RevealTimer。
- **玩家复活后仍是死亡姿态但能跑能跳**: C++ 复活已恢复输入/移动/碰撞, 但死亡表现(蓝图 BP_OnDeath 设的死亡动画/隐藏)没有对应恢复 → **动画状态机未切回**。修复: `Multicast_OnRevive` 加 `StopAllMontages(0)` + 新增 **BlueprintImplementableEvent `BP_OnRevive`**(全端触发)——**需用户在 BP_ShooterCharacter 里实现 BP_OnRevive**(切回 Locomotion 状态/恢复 mesh), 对应 BP_OnDeath 的逆操作。C++ 只提供事件+基础恢复。
- **构建**: 一次通过, DLL 14:31 (742KB), BP_OnRevive 生成确认。

## 子弹降频 + 复活 mesh/相机 C++ 兜底 (15:00)
- **子弹卡顿**: NetUpdateFrequency 200 → 100(池化多子弹同时高频复制带宽爆)。飞行仍平滑(位置快照 10ms 一次)。
- **"人物横在地面走"**: 死亡 BP 的 ragdoll 恢复不完整(mesh relative transform/物理没复位)→ **C++ Multicast_OnRevive 强制兜底**: 记录 InitialMeshTransform/InitialMeshCollisionProfile(BeginPlay), 复活时 SetSimulatePhysics(false) + 恢复 profile + 清线/角速度 + AttachToComponent(Capsule, KeepRelative) + SetRelativeLocationAndRotation(初始值)。
- **"切不回第一人称"**: Death Camera 遗留 → **C++ 复活强制夺回视角**: 遍历 GetComponents<UCameraComponent> 把非 FP 相机 SetActive(false) + FP 相机 SetActive(true) + PC->SetViewTarget(this)。
- **蓝图 BP_OnRevive 现在只需**: AnimBP 状态机变量复位(IsDead=false 等), ragdoll/相机交给 C++。
- **构建**: UBA 幻影, 手动编 8 文件 + 手动 link, DLL 15:00 (743KB)。

## 复活视角残留根因: SpringArm 第三人称相机 (15:14)
- **线索**: BP_ShooterCharacter 蓝图有 `SpringArm`/`SpringArm_GEN_VARIABLE`(第三人称跟随相机)+ `Death Camera` 变量。死亡时蓝图切到第三人称/死亡相机视角, 复活未切回。
- **真正的坑**: SpringArm 自带相机**不是 UCameraComponent** → 上一轮 `GetComponents<UCameraComponent>` 遍历 deactivate 漏掉它 → 复活后第三人称相机仍 active → "以脚为原点左右旋转"(第三人称跟随相机绕角色转)的怪画面。
- **修复(Multicast_OnRevive)**: ① 遍历 GetComponents<USpringArmComponent> → GetCameraComponent()->SetActive(false)(关掉 SpringArm 内建相机); ② 恢复 FirstPersonMesh/FirstPersonCameraComponent 的相对位姿(新增 InitialFPCameraTransform/InitialFPMeshTransform 记录于 BeginPlay); ③ FP 相机 SetActive(true) + PC->SetViewTarget(this)(加日志: old target)。
- **蓝图侧**: BP_OnRevive 现在基本无需接线(AnimBP 无死亡状态; ragdoll/相机/输入全由 C++ 兜底), 可留空或只做音效/特效。
- **构建**: UBA 幻影, 手动编 7 文件 + 手动 link, DLL 15:14 (742KB)。

## ForceFirstPersonView 兜底 + 蓝图重合消除 (15:35)
- **蓝图重合确认**: BP_ShooterCharacter 的 BP_OnRevive 已接: K2_AttachToComponent / K2_SetRelativeLocation / K2_SetRelativeRotation / SavedViewOffset——与 C++ 复活恢复重合且会覆盖 C++(BP_OnRevive 在 C++ 恢复后调用)。
- **修复**: 相机/FP 复位逻辑封装为 `ForceFirstPersonView()`(deactivate 所有非 FP UCameraComponent + 恢复 FP mesh/camera 相对位姿 + SetViewTarget(self)); Multicast_OnRevive 中调用 → BP_OnRevive(蓝图) → **延迟一帧再调一次**(最终兜底, 蓝图怎么改都会被拉回标准第一人称)。
- **编译踩坑**: FirstPersonMesh/FirstPersonCameraComponent 是 AFPSCharacter 的 **private** → 子类必须用 `GetFirstPersonMesh()`/`GetFirstPersonCameraComponent()` 访问器; SpringArm 无公共 GetCameraComponent → 删专门循环(SpringArm 内建相机就是 UCameraComponent, 已被 GetComponents<UCameraComponent> 覆盖)。
- **蓝图侧建议**: BP_OnRevive 只留 SetAnimRevive + 音效/特效, 删 attach/SetRelative/SavedViewOffset/相机操作(C++ 兜底)。
- **构建**: 修复后一次通过, DLL 15:35 (742KB)。

## AI 复用 pawn 复活 + 保底补位 + 出生点日志 (15:55)
- **AI 复用复活(需求)**: NPC::Die 不再 DeferredDestruction(销毁+重建), 改为 2s 后 `Revive()`(复用 pawn): 回出生点(FindTeamPlayerStart)、HP 回满(100)、恢复移动/碰撞/可见性、重新无敌、`Multicast_OnRevive`(撤销 ragdoll: 关模拟物理+恢复 profile/位姿/清速度+AttachToComponent+StopAllMontages)。BeginPlay 记录 InitialMeshTransform/InitialMeshCollisionProfile(所有端)。
- **补位机制重构**: 移除 OnPawnDeath 广播 + OnBotDied 补位(bot 死亡会 double 补); GameMode 加 `EnsureRosterSize` 保底(每 5s: 该队 人类+存活bot < TeamSize 才补, 覆盖 bot 被销毁的异常)。
- **出生点日志**: FindTeamPlayerStart 输出 `(slot %d/%d)` 候选总数 + 无点警告——用于定位"红蓝挤一点"(大概率是某些单位 FindTeamPlayerStart 返回 nullptr 复活原地, 或候选数==1)。
- **子弹位置差异**: NetUpdateFrequency 100→150(折中, 减小各端位置差)。网络复制固有延迟差异仍存在(各端看到服务器快照的时刻不同), 完全一致需客户端本地模拟(未做)。
- **构建**: 一次通过(修了 NPC 无 MaxHP→硬编码 100), DLL 15:55 (751KB)。

## 日志诊断: 伤害正常 + 出生点挤点根因 = World Partition 流送 (16:00)
- **蓝队复活后伤害(日志证据)**: BP_ShooterCharacter_C_1(蓝队)复活后打红队玩家 C_0: PROJ_HIT/DMG → TakeDamage 500→475→450→…→275, 全程生效。**"打不动"大概率是红队目标在出生无敌 3s(需求)**, 非 bug。
- **出生点挤点根因(实锤)**: 日志 `FindTeamPlayerStart - team 0 -> PlayerStart_0 (slot 0/1)` → **候选总数=1**! Lvl_Shooter 是 World Partition, 8 个 PlayerStart 分散地图, 只加载了玩家附近的 1 个 → GetAllActorsOfClass 只找到 1 个 → 全队挤同一个点。蓝队 SpawnBot 走 tag 回退(PlayerStart 带 RED/BLUE tag, 无日志)也受流送影响。
- **修复方向**: 关卡操作——把 8 个 PlayerStart 设为 **Always Loaded**(World Partition 常驻), 代码无需改(逻辑对)。已给 tag 回退分支补日志(`TAG fallback`)便于下次确认。
- **构建**: 一次通过, DLL 15:56 (757KB)。

## 出生点配置方案(绕开 World Partition 流送) (16:10)
- **实锤**: Lvl_Shooter 是 WP, 出生点只加载玩家附近 1 个(slot 0/1)。
- **修复**: GameMode 新增 `RedSpawnLocations`/`BlueSpawnLocations`(TArray<FTransform>, 每队 4 个)——BeginPlay 时 SpawnActor 常驻不可见 PlayerStart(不受流送影响), FindTeamPlayerStart 优先用配置(日志 cfg slot), 空则回退扫描+tag。**用户需在 BP_TDMGameMode 填 8 个出生点坐标**。
- **血量统一**: 玩家 MaxHP 500→100(与 AI 对等, 4 枪击杀), 文档 §5 已更新。
- **构建**: 一次通过, DLL 16:08 (758KB)。

## 出生点坐标自动读取 + 写入 C++ 构造 (16:20)
- **坐标来源**: 用 UnrealEditor 命令行 `-ExecutePythonScript` 启动编辑器, 脚本打开 Lvl_Shooter 遍历 PlayerStart 打印坐标(8 个全拿到)——**命令行编辑器卡死需手动结束进程**(taskkill/Stop-Process 均无效, 只能任务管理器)。
- **坐标已硬编码**到 AShooterTDMGameMode 构造的 RedSpawnLocations/BlueSpawnLocations(Y<0 红 1-4, Y>0 蓝 5-8; PlayerStart3/7 为高台 292/262): 完全绕开 World Partition 流送。运行时 BeginPlay SpawnActor 常驻不可见 PlayerStart(日志 `configured spawns resolved`), FindTeamPlayerStart 优先用(cfg slot)。
- **踩坑**: ①命令行启动编辑器后 quit 未生效→进程卡死锁 DLL→LNK1104; ②外部 actor uasset 不含世界坐标(在 umap placement 里), 只能靠编辑器 Python 读取。
- **构建**: 一次通过, DLL 16:20 (762KB)。

## 投射物持久化/下坠/回滚修复 (16:28)
- **持久化太久**: NotifyHit 命中后立即 `StopMovementImmediately` + `SetActorHiddenInGame(true)`(不可见), **1s 后回池**(原来 5s 且命中后还在飞); 激活时加 **3s 未命中超时回池**(子弹飞出地图不再永久滞留)。复用 DestructionTimer(激活 3s / 命中 1s)。
- **爆弹枪下坠**: 投射物 `ProjectileMovement->ProjectileGravityScale = 0`(无重力, 直线飞向目标)。
- **回滚表现**: 命中即停+隐藏(不再"命中后继续飞/被拉回"), 客户端同步消失。
- **构建**: 一次通过, DLL 16:28 (762KB)。

## 爆弹枪"瞄准位置不对"根因: MuzzleSocketName 为空 → 起点退化到角色根部 (16:33)
- **用户澄清**: 手雷抛物线是设计(保留重力), 问题在 DS 权威化后**初始瞄准方向就不对**。
- **根因**: `MuzzleSocketName` 默认**空**(ShooterWeapon.h 无默认值) → `GetMuzzleWorldLocation()` 的 `DoesSocketExist(NAME_None)`=false → 回退 `GetActorLocation()`(**角色根部/脚部**) → 手雷从脚部抛出 + 重力 → 落点低于准星("瞄准位置不对")。手枪直线快不受影响。
- **修复**: ①撤销 ProjectileGravityScale=0(保留蓝图重力配置, 手雷抛物线恢复); ②`GetMuzzleWorldLocation()` 无 socket 时回退**角色眼睛位置**(GetActorEyesViewPoint)而非角色根部——起点高度≈准星, 手雷落点对准 AimTarget。
- **构建**: 一次通过, DLL 16:33 (762KB)。

## 池化子弹"从别处射过来"根因: 客户端本地模拟 (16:39)
- **用户反馈**: 手枪子弹客户端表现从其他地方射过来; 爆弹枪正常/不正常交替。
- **根因**: 复制的投射物, 客户端 `ProjectileMovement` **默认自动模拟**(远端组件模拟)。池化复用时客户端实例在**旧位置**就开始本地模拟飞行(旧方向/旧位置), 服务器新位置(枪口)到达前已飞出 → "从别处射过来"; 爆弹枪则交替正常/异常。
- **修复**: `BeginPlay` 客户端分支**禁用 ProjectileMovement tick + StopMovementImmediately**——客户端只渲染服务器复制的飞行位置(150Hz 快照), 不再本地模拟。命中/激活/消失全由服务器驱动。
- **构建**: 一次通过, DLL 16:39 (762KB)。

## 弃用对象池, 改每发新建投射物 (16:46)
- **用户反馈**: 池化后跳帧 + 很多子弹看不见 + 别处有幽灵子弹在动——池化复用与复制可见性/位置在客户端无法可靠同步(复用实例从旧位置起、隐藏/显示/位置三属性复制乱序)。
- **方案**: 放弃 actor 池化(代码保留未用), FireProjectile 直接 SpawnActor(初始 transform=枪口, 客户端生成即正确, 无复用残留)。投射物: 客户端**本地模拟**(ProjectileMovement tick, 平滑无跳帧; 方向=复制的 spawn rotation, 两端一致) + **客户端碰撞 QueryOnly**(不触发本地 NotifyHit, 命中/隐藏/销毁全由服务器驱动, 不提前消失) + 服务器 BeginPlay 加 3s 未命中超时销毁 + 命中后 1s 销毁(ReturnToPool 改为 Destroy)。
- **构建**: 一次通过, DLL 16:46 (762KB)。若后续仍卡顿再考虑优化 SpawnActor 开销。

## 高速子弹防隧穿: 子步 sweep + 服务器路径检测 (16:52)
- **用户反馈**: 3000cm/s 子弹有时直接穿过目标。
- **根因**: ProjectileMovement 默认 MaxSimulationTimeStep=50ms → 每子步 150cm, 大于 34cm 半径 capsule → 物理 sweep 直接跳过目标(隧穿)。
- **修复(双保险)**: ① `MaxSimulationTimeStep=1/120`(8ms→~24cm/步) + `MaxSimulationIterations=8`(覆盖 30Hz 服务器帧)——引擎物理 sweep 不再隧穿; ② 投射物 Tick(服务器)每帧对"上帧位置→本帧位置"做 `SweepSingleByChannel(ECC_WorldDynamic, 球半径=16cm)`——低帧率兜底, 命中逻辑与 NotifyHit 统一走新抽的 `HandleImpact`(snap 到撞击点/停止/伤害/隐藏/1s 销毁)。客户端不受影响(继续本地模拟, 不跑 sweep)。顺序兼容: 组件未先 tick 时用 Velocity 预测本帧位移。
- **构建**: 一次通过, DLL 16:52 (764KB), 符号验证。

## 子弹数改为复制属性 + 客户端 OnRep 事件 (18:50)
- **需求**: 子弹数做属性同步; 客户端发子弹数变化事件。
- **实现**: ShooterWeapon::CurrentBullets 改 `UPROPERTY(VisibleAnywhere, BlueprintReadOnly, ReplicatedUsing=OnRep_CurrentBullets)` + `GetLifetimeReplicatedProps/DOREPLIFETIME`; `OnRep_CurrentBullets()` 客户端本地调 WeaponOwner->UpdateWeaponHUD → OnBulletCountUpdated 广播(事件在客户端发出)。Multicast_OnFire 保留即时 HUD 刷新(幂等), OnRep 负责权威校正+换弹同步。
- **UHT 坑(记录)**: ① `ReplicatedUsing` 的 OnRep 函数必须 **UFUNCTION()**(UHT FindFunctionByName 只找有反射的函数, 普通成员报 "Replication notification function not found"); ② ReplicatedUsing 属性必须暴露(VisibleAnywhere/EditAnywhere), 否则 "Property has a Category set but is not exposed" 报错。
- **构建**: UBA 幻影(13 文件集体报错无详情), 手动编 13 文件全 0 错误 + 手动 link, DLL 18:50 (766KB)。

## CurrentHP 事件主控端化 + 统一 HUD 事件过滤 (19:10)
- **需求**: CurrentHP 同款改造(实际已 ReplicatedUsing=OnRep_CurrentHP ✓); 接收事件的 PC 必须在客户端且为主控端。
- **实现**: ShooterCharacter 新增 `BroadcastHealthRatio(Ratio)` / `BroadcastAmmo(Magazine, Current)` 两个辅助函数, 内部 `if (!IsLocallyControlled()) return;` 才 Broadcast——**事件只在主控端客户端发出**。替换全部 7 处裸广播: BeginPlay(满血)、TakeDamage、OnRep_CurrentHP、UpdateWeaponHUD、OnWeaponActivated、死亡清零(Multicast_OnDeath)、复活刷新(Multicast_OnRevive)。武器 OnRep_CurrentBullets 走 UpdateWeaponHUD(已过滤)。PC::SetPawn 强制刷新加 `IsLocalController()` 保护。
- **效果**: 模拟端(看别人的角色)/服务器不再触发 HUD 事件; 事件接收方(PC::OnBulletCountUpdated/OnPawnDamaged → UI)只有主控端 PC 收到。
- **构建**: UBA 幻影(13 文件), 手动编全 0 错误 + 手动 link, DLL 19:10 (767KB)。

## UI 收不到事件修复: 撤销发送端 IsLocallyControlled 过滤 (19:24)
- **症状**: 子弹/血条 UI 收不到事件(上一轮加了 IsLocallyControlled 过滤后)。
- **根因**: 复制的 OnRep(OnRep_CurrentHP/OnRep_CurrentBullets)在客户端**早于 Possess/OnPossess 订阅**触发——此时 GetController() 为空 → IsLocallyControlled()=false → 过滤吞掉所有早期事件, UI 收不到。
- **正解**: 接收端订阅机制本身已保证"只有主控端 PC 收到"(PC 在 OnPossess 只订阅自己 pawn 的 delegate; 模拟端角色 delegate 无订阅者; 服务器广播无接收者)。**发送端无需过滤**——撤销 BroadcastHealthRatio/BroadcastAmmo 的 IsLocallyControlled 门。
- **初始值补发**: PC OnPossess 订阅后主动拉取: 血条 Broadcast(1.0f) + 新增弹药同步(GetCurrentWeapon() → OnBulletCountUpdated.Broadcast)——覆盖 OnRep 早于订阅丢失的初始值。
- **经验**: 复制属性 OnRep 触发时机早于 Possess; "事件只给主控端"靠订阅端(PC OnPossess AddDynamic)保证, 不要在发送端用 IsLocallyControlled 过滤。
- **构建**: UBA 幻影, 手动编 13 文件 0 错误 + 手动 link, DLL 19:24 (767KB), 符号验证。

## 弹药初始 UI 确定性修复: 武器客户端就绪后主动推送 (19:31)
- **DS 权威化后弹药初始值丢失的完整链路(已走通验证)**: ① OnRep_CurrentWeapon(武器引用复制时)触发早于 PC OnPossess 订阅 → 丢; ② OnRep_CurrentBullets 首次不触发(客户端 BeginPlay 本地设 CurrentBullets=MagazineSize == 服务器复制值); ③ PC OnPossess 补发时武器 actor 尚未复制(GetCurrentWeapon()=null)。→ 初始弹药 UI 空, 直到开第一枪才显示。
- **确定性修复**: 武器 BeginPlay 客户端分支 SetTimer 0.1s 后主动 `WeaponOwner->UpdateWeaponHUD(CurrentBullets, MagazineSize)`。时序证明必达: 武器复制必然晚于角色 possess → 推送时 PC 已订阅 + BulletCounterUI 已存在(SetupInputComponent 创建)。NPC 武器走空实现无副作用。
- **完整事件链(DS)**: 血条 = OnPossess 补发(1.0f) + OnRep_CurrentHP 后续; 弹药 = 武器主动推送(初始) + OnRep_CurrentBullets(开火/换弹) + OnRep_CurrentWeapon(换枪)。接收端=PC OnPossess 订阅自己 pawn 的 delegate(天然主控端)。
- **构建**: UBA 幻影, 手动编 13 文件 0 错误 + 手动 link, DLL 19:31 (768KB)。

## UI 事件排查: 全链路日志插桩 (19:43)
- **症状**: UI 始终不显示, 需定位断点。已加 AMMO_UI/HP_UI 前缀日志(所有端):
  - 武器: BeginPlay(client) / initial-push timer / OnRep_CurrentBullets
  - 角色: UpdateWeaponHUD / BroadcastAmmo / BroadcastHealthRatio / OnRep_CurrentWeapon
  - PC: OnPossess(含 local/Cast/force-sync) / OnBulletCountUpdated / OnPawnDamaged(含 UI 有效性)
- **待用户跑 PIE 贴日志**, 按链路定位: 武器生成→推送→角色广播→PC 订阅/接收→UI。若 handler 收到但 UI 不显示 = 蓝图 BP_UpdateBulletCounter/BP_Damaged 问题。
- **构建**: UBA 幻影, 手动编 4 文件 + 手动 link, DLL 19:43 (773KB)。

## UI 事件根因最终定位: 客户端 PC 的 OnPossess 不执行 → 订阅移到 SetPawn (19:54)
- **日志铁证**: PIE 日志只有服务器端 `PC OnPossess local=0`; 客户端角色 controlled=1 在广播(UpdateWeaponHUD/BroadcastAmmo), 但**客户端 PC 的 OnPossess 从未执行** → 从未订阅 → 事件无人接收。服务器 PC 收到事件 UI=NULL(无害)。
- **UE 机制根因**: `OnPossess` 只在**服务器**调用(服务器 Possess→ClientRestart→客户端只走 **SetPawn**, 不调用 OnPossess)。DS 权威化后订阅写在 OnPossess = 客户端永不订阅。
- **修复**: 订阅(OnBulletCountUpdated/OnDamaged/OnDestroyed)+ 主控端补发(血条1.0+初始弹药)全部移到 **`SetPawn` override**(服务器经 OnPossess 内部调用, 客户端经 ClientRestart 调用, 两端必执行); RemoveDynamic+AddDynamic 防重复订阅; OnPossess 只留服务器职责(SetTeam+TDM flow)。
- **经验(重要)**: 客户端 PC 的 pawn 相关初始化(订阅 delegate/创建 UI 依赖)必须用 `SetPawn` 或 `OnRep_Pawn`, 不能用 OnPossess(服务器专用)。
- **构建**: UBA 幻影, 手动编 13 文件 0 错误 + 手动 link, DLL 19:54 (776KB)。

## 换弹时间 (20:05)
- **需求**: 子弹打空不再立即满弹, 各武器加换弹时间: 手枪 0.5s / 步枪 0.8s / 爆弹枪 1s。
- **实现**: 武器加 `ReloadTime`(EditAnywhere, 默认 0.5s) + `bIsReloading` + `ReloadTimer`; FireProjectile 打空 → `StartReload()`(服务器, SetTimer ReloadTime 后 `FinishReload()` 满弹, CurrentBullets 复制→客户端 HUD 满弹); Fire 开头 `bIsReloading` 阻止开火; BeginPlay 按类名兜底(类名含 Grenade→1.0 / Rifle→0.8, 仅当蓝图未显式覆盖 0.5 默认值); EndPlay 清 timer。蓝图仍可在 Details 覆盖 ReloadTime。
- **构建**: UBA 幻影, 手动编 13 文件 0 错误 + 手动 link, DLL 20:05 (777KB), StartReload/FinishReload 符号验证。

## 换弹状态属性同步 (20:14)
- **需求**: 换弹中的状态同步给客户端, 用户接 UI。
- **实现**: 武器 `bIsReloading` → `ReplicatedUsing=OnRep_IsReloading` + DOREPLIFETIME; 客户端 OnRep → `NotifyReloadState()` → 角色 `OnReloadStateChanged` delegate(BlueprintAssignable); StartReload/FinishReload 服务器也调 NotifyReloadState(本地/ListenServer)。PC SetPawn 订阅 OnReloadStateChanged → handler → `BulletCounterUI->BP_OnReloadStateChanged(bool)`(新 BlueprintImplementableEvent)。
- **用户接 UI**: 在 BulletCounterUI 蓝图实现 `OnReloadStateChanged` 事件(显示/隐藏换弹指示); 或直接读武器 BP 的 Is Reloading(BlueprintReadOnly 复制属性)。
- **构建**: 一次通过, DLL 20:14 (UHT OnRep_IsReloading 生成)。

## 需求文档 v1.3 (20:30)
- **需求变更(用户裁定)**: ①不做比分栏翻转(界面标注红/蓝队); ②不做配枪阶段(固定默认手枪); ③不加换弹动画音效(没资源); ④**新增准备阶段**: 首位玩家进场后 30s 倒计时, 期间不产生 AI、全单位禁伤; ⑤**新增登录场景**: 默认显示开始菜单, 点击开始游戏后搜寻/连接 DS(默认本机 127.0.0.1, 可输 IP:端口)。
- **文档已升 v1.3**: §2.1 登录场景、§2 准备阶段流程、§4 准备期不产 AI、§5 准备期禁伤、§6 固定武器+换弹、§9 队伍标注+换弹提示、§10 加 PrepareTime=30s/出生点坐标/ReloadTime、§11 删旧限制2 加换弹无动画。
- **待实现(后续)**: 准备阶段逻辑(Ready 状态机/倒计时/全局禁伤/延迟补 AI)、登录场景 Lvl_Login + 开始菜单 + 连接 DS(默认本机/端口输入)。

## 准备阶段 + 登录场景 C++ 实现 (20:52)
- **准备阶段**: GameMode 加 ETDMMatchPhase::Prepare; StartMatch/StartPlayerMatch 首位进场 → StartPreparePhase(30s=PrepareTime 倒计时, 每秒 TickPrepareCountdown→BroadcastPrepareCountdown→PC Client_OnPrepareCountdown→BP_OnPrepareCountdown(蓝图事件)); 倒计时结束 CompletePreparePhase → Playing + FillTeamRoster(延迟补 AI) + Client_OnPrepareEnded。EnsureRosterSize 加 MatchPhase==Playing 守卫(防准备期补 AI)。角色/NPC TakeDamage 加 IsPreparePhase 全伤免疫。
- **登录场景**: UShooterLoginMenu(UUserWidget 基类: ServerAddress=127.0.0.1/ServerPort=7777 蓝图读写; StartGame() BlueprintCallable → OpenLevel("IP:Port", travel); BP_OnLoginStarted/BP_OnLoginFailed 蓝图事件); AShooterLoginGameMode(GameModeBase, 构造默认加载 /Game/Variant_Shooter/Blueprints/Login/BP_LoginMenu, BeginPlay 创建菜单+UIOnly 输入模式+显示鼠标)。
- **UI 预留接口**: 倒计时=PC 蓝图事件 BP_OnPrepareCountdown(int)/BP_OnPrepareEnded(); 登录=BP_LoginMenu 蓝图(绑定输入框到 ServerAddress/ServerPort, 按钮调 StartGame)。
- **踩坑**: Client RPC 放 protected 区 → GameMode 调用 C2248, 移到 public。
- **构建**: 一次通过(第二次修复访问级别后), DLL 20:52 (813KB)。

## 准备阶段 UI 接入(PC 创建 + 事件转发) (21:24)
- **用户已建 UI**: /Game/Variant_Shooter/UI/PrePareUI(继承 UUserWidget, 无事件接口)。
- **实现**: 新增 `UShooterPrepareUI`(UUserWidget 子类, BlueprintImplementableEvent: BP_OnPrepareCountdown(int32)/BP_OnPrepareEnded()); PC 加 PrepareUIClass(默认加载 PrePareUI_C)+ SetupInputComponent 创建(AddToPlayerScreen(50), 初始 Collapsed); Client_OnPrepareCountdown → Cast<UShooterPrepareUI>(PrepareUI) → Visible + BP_OnPrepareCountdown(sec); Client_OnPrepareEnded → Collapsed + BP_OnPrepareEnded。PC 自己的 BP_OnPrepareCountdown/Ended 事件仍同时触发(兼容)。
- **用户需做**: ①PrePareUI 蓝图类设置 → 父类改为 **ShooterPrepareUI**(布局保留); ②事件图表实现 OnPrepareCountdown(更新倒计时文本)/OnPrepareEnded(清空/隐藏)。
- **构建**: 一次通过, DLL 21:24 (823KB)。

## 准备阶段结束重置: 销毁 pawn 重生 + 比分清零 (21:31)
- **需求**: 准备阶段结束后销毁玩家 pawn 重新走重生流程(出生点/满血/无敌/武器), 并初始化比分。
- **实现**: CompletePreparePhase 扩展: ①TeamScores.Reset() + 向所有客户端广播 Client_UpdateScore(0,0)/(1,0)(比分归零); ②遍历所有玩家 PC, GetPawn()->Destroy() → 触发 OnPawnDestroyed → 服务器权威重生(FindTeamPlayerStart + spawn + Possess + 出生无敌 + 武器); ③再 FillTeamRoster 补 AI + BP_OnMatchStarted + Client_OnPrepareEnded。
- **构建**: 一次通过, DLL 21:31 (825KB)。

## 队伍分配时序 + 5人超员 + 选队接口 (21:41)
- **同边复活根因**: PostLogin 里 Super::PostLogin(基类 RestartPlayer 立即 spawn 玩家)在队伍分配**之前**执行 → ChoosePlayerStart 查 GetTeam() 全是 0 → 所有玩家初始出生在红方。**修复**: 队伍分配(SetTeam/SetTeamTags/冻结)移到 Super::PostLogin **之前**。
- **5 人超员根因**: FillTeamRoster 补 TeamSize-存活bot(忽略人类) → 准备结束补满 4 bot + 已进场人类 = 5。**修复**: 补 `TeamSize - CountHumanPlayers - CountAliveBots`(人类先占位)。
- **重生新角色没队伍**: OnPawnDestroyed spawn 后加 `RespawnedCharacter->SetTeam(TeamByte)`(染色/友伤正确)。
- **选队接口**: PC 加 `ServerChooseTeam(uint8 Team)`(Server RPC, public) → SetTeam; 菜单蓝图按钮调用(0=RED/1=BLUE)。默认 PostLogin round-robin 保留, 选队覆盖; 重生/出生点均用所选队伍。
- **满弹**: 重生 spawn 新角色 → BeginPlay 生成新武器满弹(CurrentBullets=MagazineSize); 客户端 initial-push + OnRep_CurrentBullets + SetPawn force-sync 通知 UI(链路已覆盖)。
- **构建**: 一次通过, DLL 21:41 (827KB)。

## AI 行为增强: 寻敌/持续射击/30%命中率/丢失停火 (21:49)
- **持续射击**: NPC StartShooting 首枪立即 + SetTimer(AutoFireTimer, Weapon->GetRefireRate()) 循环 AutoFireTick(继续 StartFiring)——半自动手枪现在能连射; StopShooting/Die/EndPlay 清理 timer。武器加 GetRefireRate getter。
- **命中率 ~30%(扰动加大)**: NPC AimVarianceHalfAngle 默认 10° → **15°**(锥散布, 已有机制: GetWeaponTargetLocation 用 RandomUnitVectorInConeInDegrees + MinAimOffsetZ/MaxAimOffsetZ 垂直偏移)。命中率与距离相关, 15° 为起始值, 蓝图上可调(BP_ShooterNPC Details)。
- **感知敌我(关键修复)**: Sense Enemies 原来 `ActorHasTag("Player")` 过滤 → **AI 识别不到敌方 AI**(bot 无 Player tag)。改为**队伍制**: 感知到 AShooterCharacter 时比较 TeamByte(不同队=敌人, 同队/未分配=忽略)——红/蓝 bot 互打、bot 打真人 ✓。
- **丢失停火/寻路**: StateTree ExitState(ShootAtTarget)→ StopShooting(已清 timer); 感知丢失→StateTree 切回寻路(资产驱动)。
- **构建**: 一次通过, DLL 21:49 (828KB)。

## AI 不动/5人/开局同一边 — 诊断日志插桩 (21:57)
- **症状**: ①AI 不动(疑似 DS 化影响) ②每队 5 人 ③开局玩家同一边。
- **已查实**: AI 配置完整(NPC AutoPossessAI=PlacedInWorldOrSpawned + BP_ShooterAIController + ST_Shooter StateTree 含漫游/感知/射击任务; OnPossess→StartLogic)。关卡有 Dynamic RuntimeGeneration WP RecastNavMesh + NavMeshBoundsVolume。
- **已加日志**: FillTeamRoster(每队 humans/aliveBots/spawning N)、SpawnBot 后 1s 诊断(controller 是否 NULL + 位置)、AIController OnPossess(StateTree started)。PostLogin/FindTeamPlayerStart 日志已有。
- **待用户跑 PIE 贴 TDM_FLOW 日志**:
  - SpawnBot diag controller=NULL → bot 没 AI 控制器(排查 spawn/auto-possess);
  - controller=VALID 但不移动 → NavMesh(动态生成/流送)问题;
  - FillTeamRoster humans=X → 核对 5 人组成;
  - FindTeamPlayerStart team X slot → 核对开局分边。
- **构建**: 一次通过, DLL 21:57 (831KB)。

## AI 不动根因锁定: NavMesh 未生成 + 玩家1 登录无 pawn (22:03)
- **日志证据**: ①`LogCrowdFollowing: Unable to find RecastNavMesh instance`(bot spawn 后 16s 仍找不到)→ **NavMesh 没生成 → AI MoveTo 全失败 → bot 站桩**; ②RecastNavMesh maxTiles 序列化不匹配(重建); ③玩家 0 在 PostLogin 前 spawn(默认 team0), 玩家 1 登录后**无 spawn 记录**(直到准备结束才重生)。
- **修复**: ①GameMode BeginPlay 加 NavSystem 诊断(NavSystem + mainNavData 有效性, 确认 NavMesh 是否生成); ②PostLogin 后检查无 pawn → **强制 RestartPlayer 兜底**(修玩家 1 无 pawn); ③Build.cs 加 NavigationSystem 模块。
- **NavMesh 修复方向(待用户确认日志)**: 若 mainNavData=NULL → WP Dynamic RuntimeGeneration NavMesh 未构建 → 需编辑器操作(把 RecastNavMesh 改 Static 并 P 键构建, 或检查 bAllowWorldPartitionedNavMesh)。
- **5 人问题**: FillTeamRoster 日志显示 humans=1 → spawning 3(每队 4, 正确)——用户观察的 5 可能来自旧 DLL/误解, 需复核。
- **构建**: 一次通过(修 NavigationSystem 链接), DLL 22:03 (832KB)。

## Login 队伍分配修正 + NavMesh Dynamic 强制重建 (22:18)
- **Login override 修正(关键踩坑)**: 上一轮把队伍分配写进 Login 但**签名臆造错误**(FString Login(APlayerController*, const FString&))→ 编译 C3668/C2660。UE5.8 真实签名: `APlayerController* Login(UPlayer* NewPlayer, ENetRole, const FString& Portal, const FString& Options, const FUniqueNetIdRepl& UniqueId, FString& ErrorMessage)`; **PC 是基类 Login 内部创建的**(NewPlayer 是 UPlayer 不是 PC)。正确实现: 先 `Super::Login(...)` 拿返回的新 PC → 立即 SetTeam/SetTeamTags/冻结 → 返回。基类 PostLogin→RestartPlayer 在 Login 返回后才 spawn pawn → ChoosePlayerStart 读到正确队伍(修复准备阶段同边)。
- **NavMesh 方案修正(用户裁定)**: 场景有可移动物体 → **必须保持 Dynamic**, 撤回升 Static 建议。日志铁证: NavMesh 对象存在但 tile 为空(maxTiles 序列化不匹配重建后无数据 + CrowdFollowing 找不到)。C++ 兜底: BeginPlay 诊断升级(GetNumActiveTiles 实际数据数 + GetRuntimeGenerationMode), tiles==0 时 3s 后强制 `NavSys->Build()` 同步重建。
- **UE5.8 API 变化**: ARecastNavMesh::GetTilesCount 不存在 → `GetNumActiveTiles()`/`GetNavMeshTilesCount()`; RuntimeGeneration/bRebuildAtRuntime 从 RecastNavMesh 移到父类 ANavigationData(GetRuntimeGenerationMode(), bRebuildAtRuntime 已废弃); 头文件 NavMesh/RecastNavMesh.h。
- **字符串验证技巧**: UE5 编译产物字符串是 UTF-16(每字符后 \0), ASCII regex 搜不到; 用 `re.findall(rb'(?:[\x20-\x7e]\x00){8,}')` + utf-16-le 解码。
- **构建**: 一次通过(修正签名后), DLL 22:18 (834KB), obj 验证 3 条新日志字符串。

## AI 不动第2轮: SetTeam 时序 bug + MoveDiag 诊断 (22:23)
- **日志发现**: ①Login 队伍分配修复生效(PC 0/1 各就各位)✓; ②NavMesh tiles=30 runtimeGen=2(Dynamic 有数据, 强制 Build 未触发)✓; ③**AIController possessed 时 team 255**——SpawnActor 内部 AutoPossessAI 触发 OnPossess/StateTree StartLogic 时 SetTeam 还没执行(bot spawn 返回后才 SetTeam)→ 感知敌我判断用 255。
- **修复**: SpawnBot 改 `SpawnActorDeferred` + 先 SetTeam/GrantSpawnProtection 再 `FinishSpawning`——possess 时队伍已就位。
- **MoveDiag 诊断**: bot spawn 后 2/4/6/8/10/12s 打印 `speed= vel= loc=`——区分"没开始寻路(speed=0)" vs "寻路但卡住(speed>0 位置不变)"。
- **CrowdFollowing 警告**(Unable to find RecastNavMesh while creating UCrowdManager): AI 控制器用普通 PathFollowingComponent(非 DetourCrowd), 此警告疑似无害, 待 MoveDiag 确认。
- **构建**: 一次通过, DLL 22:23 (837KB)。

## AI 不动第3轮: MoveDiag 实锤 speed=0 + TEST-MoveTo 二分实验 (22:28)
- **MoveDiag 决定性证据**: 6 个 bot 出生后 2-12s 全程 `speed=0`, 位置完全不变 → **StateTree 从未产生移动指令**(不是"寻路卡住", 是"没开始寻路")。SetTeam 时序修复已生效(possessed 全 0/1)。
- **已排除**: NavMesh tiles=30 runtimeGen=2(Dynamic 有数据); EQS 资产存在(EQS_FindRoamLocation); StateTree 结构完整(Move to Roam/Find Roam/EQS RandomBest25Pct/Idle/Investigate/Snipe/Sense/Shoot 全有)。
- **TEST-MoveTo 二分实验(本轮)**: bot spawn 后 3s 直接 `AIC->MoveToLocation(当前位置+5m)` 绕开 StateTree——result: 0=Failed(寻路问题)/1=AlreadyAtGoal/2=RequestSuccessful(成功→问题在 StateTree 任务链 EQS/绑定)。待用户跑 PIE 看日志。
- **构建**: 一次通过(修 C2440 GetController 返回 AController 需 Cast), DLL 22:28 (839KB)。

## AI 不动第4轮: TEST-MoveTo result=2 但 speed=0 → 执行层问题 (22:32)
- **二分结果**: 6 bot 直接 MoveToLocation(+5m): 4 个 result=2(请求成功), 2 个 result=0(C_3/C_4 蓝队, 出生点不可达), **但 4 个 result=2 的 bot 速度依然全 0 → 路径请求成功却没执行**。
- **排除**: GrantSpawnProtection 不禁移动; NavMesh tiles=30 正常。
- **本轮加**: TEST-MoveTo 打印 moveStatus(0=Idle 1=Wait 2=Pause 3=Moving) + moveMode(0=None 1=Walk 2=NavWalk 3=Fly); 6s 后 TEST-Check 复查 speed/moveStatus/moveMode/loc——确定是 "PathFollowing 不推进" 还是 "MovementMode=None 无法执行"。
- **待用户跑 PIE 贴日志**。
- **构建**: 一次通过, DLL 22:32 (841KB)。

## AI 不动第5轮: 实锤移动系统正常, 问题在 StateTree 任务链 (22:36)
- **TEST-Check 决定性证据**: 4 个 result=2 的 bot 3 秒内移动 466cm(C_1: -1050→-584, C_0: -691→-226, C_2: -1450→-984, C_5: +803→+1269), moveStatus=3(Moving) → **直接 MoveTo 完全正常**。之前 MoveDiag speed=0 是 StateTree 从未发指令(绕开 StateTree 就动)。
- **结论**: 移动/寻路系统 OK; **StateTree 状态机未发出漫游移动指令**(EQS Find Roam / Move to Roam 任务链断)。
- **已加 STTASK 日志**: 4 个 C++ 任务(FaceActor/FaceLocation/ShootAtTarget/SenseEnemies)EnterState 打日志——状态机若推进到这些任务会打印; 若全无 → 卡在资产内任务(EQS/漫游/Delay)。
- **另发现**: C_3/C_4 蓝队高台出生点 MoveTo result=0(不可达/不在导航区), 需调整出生点或 navmesh。
- **构建**: 一次通过, DLL 22:36 (841KB)。

## AI 不动第6轮: 用户猜测方向正确 — StartLogic 时机太早 (22:45)
- **用户提示**: "AIController 没生成并 Possess?" — 日志否定(OnPossess 执行 + MoveTo 可用), 但方向正确: **OnPossess 在 SpawnActor 的 AutoPossess 流程中触发, 早于 AIController 的组件初始化(BeginPlay)**, 此时 `StartLogic()` 静默 no-op → 状态机从未运行 → STTASK=0 + bot 不动。
- **修复**: StartLogic 从 OnPossess **移到 AIController::BeginPlay**(组件初始化后可靠启动); OnPossess 只留 possessed 日志; BeginPlay 打 `isRunning/runStatus` 确认。
- **踩坑**: ①StateTreeComponent.h 在 Components/ 子目录(include "Components/StateTreeComponent.h"); ②替换代码丢了大括号(C2601/C1075), 手工补齐; ③UStateTreeComponent 无 GetStatus/GetStateTree/GetInstanceData 访问器(上一轮诊断用的 API 不存在, 本轮改用 IsRunning()/GetStateTreeRunStatus())。
- **验证点(待 PIE)**: `BeginPlay - StartLogic (isRunning=1)` + `STTASK: SenseEnemies EnterState` + MoveDiag speed>0。
- **构建**: 一次通过(修括号后), DLL 22:45 (842KB)。

## AI 不动全链路排查确认(用户洗澡期间, 22:50)
- **已确认正常(代码/日志证据)**: ①AIController 生成+Possess(OnPossess 日志); ②StateTreeRef=/Game/.../ST_Shooter 资产引用配置✓; ③AIPerception Sight/Damage/Hearing 三感知✓; ④NavMesh tiles=30 Dynamic✓; ⑤移动/寻路系统(TEST-MoveTo 466cm/3s)✓; ⑥EQS_FindRoamLocation(SimpleGrid+Overlap+Pathfinding+RandomBest25Pct)✓; ⑦队伍时序(SpawnActorDeferred)✓。
- **已修根因(待验证)**: StartLogic 在 OnPossess 时机太早(AutoPossess 于 SpawnActor 内部触发, 组件未初始化)→ 静默失败 → 状态机从未运行(STTASK=0)。修复: 移到 AIController::BeginPlay + isRunning/runStatus 日志。DLL 22:45。
- **遗留问题**: ①蓝队高台出生点(PlayerStart6/7) TEST-MoveTo result=0 不可达 → 需改出生点或 navmesh; ②MoveDiag/TEST-MoveTo/TEST-Check 测试代码验证后移除; ③bot 死亡 2s 内 StateTree 持续运行(Die 不停逻辑), 复活后继续; ④CrowdManager 警告疑似无害(PathFollowing 不依赖); ⑤SenseEnemies EnterState 直接解引用 Controller 无空检查(资产绑定损坏会崩)。
- **验证方法**: PIE 看 `BeginPlay - StartLogic (isRunning=1)` + `STTASK:` + `MoveDiag speed>0`。

## AI 不动第7轮: "动一下"是 TEST-MoveTo 假象 + 确认 ST_Shooter 空树 (23:09)
- **日志**: 仍 runStatus=4(Succeeded), STTASK=0。**引擎源码铁证**: GetStateTreeRunStatus 在无执行状态时返回 Failed(3), 而日志是 4 → **树实例化成功但启动即完成 = 树内容为空(CompactStateTree 运行数据缺失, 资产序列化损坏)**。用户"动了一下"是 3s 时 TEST-MoveTo(+5m) 造成的假象, "卡顿"也是 6 bot 同时寻路所致。
- **已做**: 移除全部测试代码(MoveDiag 循环/TEST-MoveTo/TEST-Check + 相关 include)——消除假象与卡顿。保留: SpawnBot diag(1s)、BeginPlay isRunning/runStatus、STTASK(4 任务 EnterState)。
- **待用户(方案 A)**: 编辑器打开 ST_Shooter → Compile → 保存 → 重新生成 CompactStateTree 运行数据 → PIE 验证 isRunning=1。
- **备选(方案 B)**: 若重存无效, C++ 兜底 AI(漫游+索敌+射击, 不依赖 StateTree, 复用已验通的移动/感知/射击)。
- **构建**: 一次通过, DLL 23:09 (836KB)。

## 方案 B 实施: C++ 兜底 AI(绕开损坏的 ST_Shooter) (23:22)
- **背景**: 重存 ST_Shooter(方案 A)后仍 runStatus=4 → 资产运行时数据彻底损坏, 用户决定"以后再查", 先上方案 B。
- **实现**(AShooterAIController 重写): BeginPlay 不再 StartLogic(空树), 改用 **Tick 驱动 C++ AI**(服务器权威): ①FindEnemy(0.4s 节流): 遍历 AShooterCharacter, 不同队+未死+距离<SightRadius(4000)+视线检测(ECC_Visibility, 85cm 高), 选最近; ②UpdateCombatBehavior: 有敌→面向(SetControlRotation 含俯仰)+ 距离管理(>1200 靠近 MoveToActor / <=1200 停住射击)+ 复用 NPC StartShooting(持续连射+15°散布=30%命中率); ③无敌→StopShooting + 漫游(GetRandomPointInNavigableRadius(1500) + MoveToLocation, 2.5s 冷却)。OnPawnDeath 加 StopShooting。
- **可调参数**(BP_ShooterAIController Details): SightRadius/EngageMinDistance(350)/EngageMaxDistance(1200)/RoamRadius(1500)/AimScanInterval(0.4)/RoamPauseTime(2.5)。
- **踩坑**: ①头文件编辑误删 OnShooterPerceptionUpdated/Forgotten delegate 声明(StateTree 任务还用)→ 补回; ②GetRandomPointInNavigableRadius 第三参数是 FNavLocation&(不是 FVector&)。
- **构建**: 一次通过(两处修复后), DLL 23:22 (844KB), fallback 字符串验证。

## 选队接口 UMG 友好化: UShooterTDMMainMenu::SelectTeam (23:32)
- **用户反馈**: ServerChooseTeam(Server RPC)在 UMG 蓝图里搜不到——RPC 函数需要 PC 对象上下文(先 Get PC → Cast)才可见, 直接搜索无结果。
- **改进**: UShooterTDMMainMenu 加 `UFUNCTION(BlueprintCallable) SelectTeam(uint8 Team)`——内部 Cast<AShooterPlayerController>(GetOwningPlayer()) → ServerChooseTeam(Team)。UMG 里直接搜 **Select Team** 即可(菜单自身方法), 无需拼 PC 引用。BP_TDMMainMenu 继承该类 ✓。
- **构建**: 一次通过, DLL 23:32 (845KB)。

## 打包排查(23:40-23:50)
- **打包命令**: RunUAT BuildCookRun -platform=Win64 -clientconfig=Development -allmaps -archive(Package/) **必须带 -NoUBA**(UBA 9001 幻影)。
- **LNK1201 FPS.pdb 锁定 ×2**: 0 字节 FPS.pdb 写入失败。处理: 删 0 字节 PDB + 清理 mspdbsrv/link/cl 残留 + 重跑。第三次打包进行中, 若仍失败 → 疑似杀毒软件拦截 PDB 写入(需加 Defender 白名单 Binaries 目录)。
- **打包前提已改**: DefaultEngine.ini 默认地图 → Lvl_Login(Editor/Game), ServerDefaultMap → Lvl_Shooter; 两关卡的 GameMode 均已在 WorldSettings 配置(Login=ShooterLoginGameMode, Shooter=BP_TDMGameMode)。

## 打包根因找到: 损坏的 FPS.lib 残留 (23:52)
- **LNK1201/LNK1136 真凶**: Binaries/Win64/FPS.lib 是 0 字节损坏残留(首次 9001 失败留下)→ 链接器覆盖它时 LNK1136(invalid or corrupt), 连带 PDB 0 字节。**清理全部 FPS.* 残留后手动链接一次成功**(FPS.exe 340MB, PDB 379MB, exit=0)。
- **经验**: 打包失败后必须清理 Binaries/Win64 下同名残留(exe/pdb/lib/exp), 否则链接器反复覆盖损坏文件失败。手动链接 FPS.exe 用 Intermediate/Build/Win64/x64/FPS/Development/FPS.exe.rsp。
- **第四次打包运行中**。
