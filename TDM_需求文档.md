# TDM 团竞模式 — 游戏需求文档

> **版本**: v1.4(最终版) | **更新日期**: 2026-08-18
> **文档原则**: 本文件是团队竞技(TDM)模式的**唯一效果基准**。后续开发/修复/评审一律以本文档为准;
> 若实现与本文档冲突,**先确认再动手,不擅自更改**。文档本身有疑问或需变更时,先与需求方确认后更新本文档并升版本号。
> **v1.4 变更**: 登录场景 UI 改由客户端 PlayerController 创建(不依赖服务器 GameMode,修复联网 PIE 下 UI 不显示);确认登录场景为客户端入口、DS 独立服务器打包受引擎 Launcher 版限制(详见 §8 附注)。

---

## 1. 游戏概述

- **模式名称**: 团队竞技 (Team Deathmatch, TDM),参考"和平精英·经典团竞"。
- **玩法**: 红蓝两队对抗,率先达到击杀目标数(KillTarget)的队伍获胜。
- **规模**: 每队 4 人(人类 + AI 混合,见 §4),共 4v4。
- **地图**: `Lvl_Shooter`,双方出生点分别在地图两边。
- **登录场景**: 游戏启动默认进入登录场景(`Lvl_Login`),见 §2.1。
- **v1.1 变更**: 新增友军伤害免疫、复用 pawn 复活(2 秒倒计时)、出生点按编号 1-4/5-8 分组、比分栏左己右敌;修复 mesh 队伍染色不生效。
- **v1.2 变更**: 出生点**顺序分配**(不堆积);修复部分单位(红方玩家/蓝队 AI)染色不生效(OnRep 首次复制触发问题)。
- **v1.3 变更**:
  - 新增**登录流程**:登录场景 + 开始菜单,点击"开始游戏"后搜寻/连接 DS(默认本机,可输端口);
  - 新增**准备阶段**:第一位玩家加入后开始 **30 秒倒计时**,准备期间**不生成 AI**、**不能造成伤害**;
  - **取消配枪浮层**:不做配枪选择,玩家固定默认武器(手枪);
  - **比分栏调整**:不做左右翻转,界面改为标注"红队"/"蓝队"识别;
  - 同步实现:修复连射瞄准点不跟随(§11 限制 2 移除)、出生点改为 GameMode 坐标配置(绕开关卡流送)、新增换弹时间与换弹状态同步。
- **v1.4 变更(最终)**:
  - 登录场景 UI 创建逻辑从 `AShooterLoginGameMode::BeginPlay`(服务器)迁移到 **`AShooterLoginPlayerController::BeginPlay`(客户端本地控制器)**,GameMode 只负责配置 PlayerControllerClass —— 修复联网 PIE/DS 下登录菜单不显示(此前 GameMode 仅在服务器进程执行,客户端永远看不到 UI);
  - 确认 **登录场景是纯客户端入口**:启动进 Lvl_Login 显示开始菜单,输入 IP:端口后 OpenLevel 连接 DS,不依赖服务器在登录场景提供任何逻辑。

## 2. 对局流程

```
登录场景(开始菜单) → 连接 DS → 主菜单(等待进场) → 准备阶段(30s 倒计时) → 对战(Playing) → 结算屏(Ended) → 返回菜单(重载关卡)
```

### 2.1 登录场景(需求)

- **登录场景 `Lvl_Login`**: 游戏启动默认进入登录场景,显示**开始菜单**:
  - 标题 + "开始游戏"按钮;
  - **服务器地址/端口输入框**。
- **连接方式**: 点击"开始游戏"后搜寻并连接 DS 服务器完成登录:
  - **默认本机**: 不输入任何内容时默认连接本机(`127.0.0.1`)作为 DS;
  - **自定义端口**: 可在输入框填写服务器 IP:端口(或仅端口),按填写内容连接。
- 连接成功后进入主游戏场景(`Lvl_Shooter`),显示 TDM 主菜单(进入主菜单阶段)。
- **实现确认(v1.4)**: 登录场景为**纯客户端入口** —— UI 由客户端 `AShooterLoginPlayerController::BeginPlay`(`IsLocalController` 判断)创建并显示,`AShooterLoginGameMode` 仅负责指定 PlayerControllerClass。联网 PIE/DS 下客户端进程没有 GameMode 逻辑,因此 UI 绝不能创建在 GameMode 里(修复前登录菜单不显示)。

### 2.2 主菜单 → 准备阶段 → 对战

| 阶段 | 行为 |
|---|---|
| **主菜单** | 玩家加入后自动冻结输入(不能移动/转向)、弹出主菜单(暗底 + "开始对战"按钮)。每个玩家**独立**点开始,谁点谁进场,不互相等待 |
| **准备阶段(需求)** | **第一位玩家点开始进场时,比赛进入准备阶段并启动 30 秒倒计时**。准备期间:① **不生成 AI**(开局不补 AI);② **不能造成伤害**(所有单位伤害免疫);③ 玩家可自由移动/熟悉地图/互相观察。后续玩家陆续进场同样处于准备状态 |
| **对战** | **30 秒倒计时结束** → 正式进入对战:按 §4 补齐 AI、**解除禁伤**、正常计分。首位进场者触发的开局逻辑推迟到此阶段 |
| **结算** | 一队击杀数达到 KillTarget → 全端弹结算屏(获胜方 + 最终比分 + "返回菜单"按钮) |
| **返回菜单** | 点击后重载关卡,回到主菜单阶段(重新分配队伍;准备阶段倒计时重新开始) |

### 关键规则:个人进场
- 点"开始"只让**点击者本人**进场:解冻该玩家、隐藏其菜单、发放武器。
- 其余玩家保持冻结在菜单,直到各自点开始。
- 中途加入的玩家同样先冻结 + 弹菜单,点开始后进场(并顶替一个 AI,见 §4)。
- 准备阶段内进场的玩家:已进场但**不能造成伤害**(禁伤),倒计时结束统一解除。

## 3. 队伍系统

- **分配方式**: 玩家加入(登录)时按 **round-robin** 交替分配队伍(第 1 个 RED、第 2 个 BLUE、第 3 个 RED……),不再更改。
- **视觉区分**: 角色/NPC 的第三人称模型按队伍染色 —— **RED = 红色(0.85, 0.15, 0.15),BLUE = 蓝色(0.15, 0.25, 0.9)**,通过 Mannequin 材质的 `Paint Tint` / `LogoTint` 参数实现;生成、重生、入场时自动应用。染色由**每个客户端本地**执行(材质实例不复制,队伍编号通过复制同步)。
- **出生点(需求)**: 出生点命名为 **Player1 ~ Player8** —— **Player1-4 为红方出生点,Player5-8 为蓝方出生点**;玩家出生/复活、AI 生成均按此分组选择。**出生点按顺序轮流使用(round-robin:Player1→2→3→4…),单位不会堆积在同一出生点**。
  - **当前实现(绕开关卡流送)**: 出生点坐标由 GameMode 配置 `RedSpawnLocations` / `BlueSpawnLocations` 提供(每队 4 个,代码内置,不依赖关卡加载状态),`FindTeamPlayerStart` 优先使用,空则回退按编号/标签扫描关卡出生点。
  - **建议(可选)**: 在关卡里把 8 个 PlayerStart 规范改名为 `Player1`~`Player8`,则严格按 1-4/5-8 分组。

## 4. AI 补位策略(核心规则)

| 时机 | 行为 |
|---|---|
| **准备阶段(30s 倒计时内)** | **不产生任何 AI**(需求) |
| **倒计时结束(进入对战)** | 两队各补满 **TeamSize 个 AI**(开局满员,即使人类为 0) |
| **玩家进场**(点开始,对战中) | 从该玩家队伍**随机移除一个存活 AI**,人类顶替其名额(队内总数保持 TeamSize) |
| **AI 死亡** | 延迟 BotRespawnDelay(3s)后补位,补位数量 = TeamSize − 人类数 − 存活AI数(人类占位后自动少补) |
| **人类中途加入** | 不额外补 AI;该玩家进场时顶替一个 AI |

- 队伍人数恒等式: **人类 + AI = TeamSize**(死亡补位窗口期除外)。
- AI 使用 `BP_ShooterNPC`(StateTree 驱动,自动索敌射击),武器为手枪。

## 5. 出生、复活与保护

- **出生无敌(需求,不可移除)**: 玩家和 AI 每次出生/重生都获得 `SpawnProtectionTime`(默认 3 秒)无敌窗口,期间不承受任何伤害 —— 用于防止出生点被压制。
  - 注意:因此**开局双方单位刚生成的前 3 秒内互相打不掉血是正常现象**,不是 bug。
- **准备阶段禁伤(需求)**: 准备阶段(30 秒倒计时)内**所有单位不承受伤害、也无法造成伤害**(全局无敌,伤害一律忽略),倒计时结束解除。防止准备期偷袭。
- **血量统一(需求)**: 玩家与 AI **最大生命值一致(默认 100)**,伤害 25/发 → **4 枪击杀**,玩家互殴与打 AI 的击杀节奏相同(避免"玩家打不死"的不对等体验)。
- **友军伤害免疫(需求)**: 同队攻击不造成任何伤害(不扣 HP、不致死、不计分),只有敌方伤害有效。
- **玩家复活(需求)**: 死亡后**不销毁、不重新生成 pawn** —— **复用原来的 pawn 原地复活**:
  1. 死亡瞬间:计分、禁用输入、播放死亡表现;
  2. **2 秒复活倒计时**(`RespawnTime`);
  3. 倒计时结束:传送回己方出生点(Player1-4 / 5-8)、HP 回满、恢复移动与碰撞、重新装备武器、重新获得出生无敌、恢复输入。
- **AI 复活**: AI 死亡后按 §4 由服务器补位生成(新 AI 单位),同样带出生无敌。

## 6. 武器系统

- **默认武器**: 手枪(`BP_ShooterWeapon_Pistol`,投射物 `BP_ShooterProjectile_Bullet`)。
- **兜底规则**: 即使配枪列表未配置,玩家也**必定拿到一把手枪**(角色内置默认武器兜底),保证任何时候能战斗。
- **固定武器(需求变更)**: **不做配枪选择浮层**,玩家固定使用默认武器(手枪)。`LoadoutWeapons` 保留但默认空;未来如需多武器,再行启用配枪/切枪。
- **切枪**: 已拥有多把武器时可切换(服务器权威)。
- **换弹(需求)**: 弹匣打空后**不立即满弹**,按武器类型耗时换弹,期间**无法开火**:
  - 手枪 `ReloadTime` = **0.5s**;步枪 = **0.8s**;爆弹枪 = **1.0s**(蓝图可在 Details 覆盖);
  - **换弹状态同步**: `bIsReloading` 为复制属性,所有客户端可见;UI 可据此显示"换弹中"提示(事件链:武器 → 角色 `OnReloadStateChanged` → 本地 PlayerController → HUD 蓝图事件 `OnReloadStateChanged`)。

## 7. 计分与胜负

- **计分语义**: 击杀后分数加在**击杀者队伍**上;自杀 / 环境伤害 / **误杀队友不计分**(且友军伤害已被免疫,见 §5)。
- **胜利条件**: 某队分数达到 `KillTarget`(默认 30)即结束,该队获胜。
- **实时比分**: 击杀瞬间广播到所有客户端 HUD 刷新(服务器权威计分)。
- **结算屏**: 显示获胜方(如"RED 获胜!")与最终比分(如"RED 12 - 30 BLUE")。

## 8. 联机架构(DS,硬性要求)

- **服务器权威(DS)**: 服务器是唯一权威 —— 移动、伤害判定、死亡、计分、重生、AI 生成全部只在服务器执行,客户端只做表现与输入上报。
  - 伤害: `TakeDamage` 只在服务器生效(客户端本地伤害一律忽略)。
  - 死亡: 服务器判定死亡并广播表现(ragdoll / 死亡动画 / 结算),所有客户端同步一致。
  - 复活: 服务器决定何时复活并传送回出生点,**复用原 pawn**(客户端不做本地复活)。
  - 开火: 客户端把**瞄准点**(含镜头俯仰)随 Server RPC 上报,服务器生成投射物、判定命中(保证"瞄哪打哪"且无法作弊)。
  - 队伍: 队伍编号复制到客户端(角色/PC),客户端据此本地染色、本地 UI 判断己方。
- **登录连接(需求)**: 客户端从登录场景点击"开始游戏"→ 连接指定 DS(默认本机,可填 IP:端口)→ 登录成功后进入主游戏场景。
- **输入冻结**: 服务器设置冻结标记并复制,客户端 UI 只负责显示。
- **UI 归属**: 菜单/结算/HUD 全部由各客户端本地创建(DS 上服务器不建 UI);状态由服务器 RPC 驱动。
- **附注 — DS 独立服务器打包(2026-08-17 排查结论)**:
  - Launcher 版(非源码)引擎默认**禁止编译 `TargetType.Server`**(`InstalledBuild.txt` 存在 → UBT 抛 "Server targets are not currently supported from this engine distribution");
  - 改名 `InstalledBuild.txt` 可放行,但会触发**全量重编译引擎**(≈2h)且 Launcher 版 ThirdParty 源码被裁剪(msdfgen/AHEasing/SSEMathFun 等缺失)→ 编译必失败;
  - **Game 目标二进制(`FPS.exe`)无法以 `-server` 进入 DS 模式**(`IsRunningDedicatedServer()` 对 `UE_GAME` 目标硬编码返回 false,编译期决定);
  - **结论**: 当前环境联机验证用 ①编辑器 `UnrealEditor.exe FPS.uproject -server`(开发期 DS)或 ②win 包 Listen Server(`FPS.exe "Lvl_Shooter?listen"`);正式独立 DS 部署需源码版引擎。

## 9. UI 一览

| 界面 | 内容 | 入口 |
|---|---|---|
| 登录菜单 `Lvl_Login` 场景 | 标题 + "开始游戏"按钮 + 服务器地址/端口输入框 | 游戏启动默认显示 |
| 主菜单 `BP_TDMMainMenu` | 标题 + "开始对战"按钮 | 玩家加入/持有 Pawn 且未进场时自动弹出 |
| 结算屏 `BP_TDMEndScreen` | 获胜方、比分、"返回菜单"按钮 | 比赛结束由服务器广播弹出 |
| HUD(基类 `UShooterUI`) | 弹药计数、队伍比分、受击反馈、换弹提示 | 玩家进场时创建 |

- **队伍识别(需求变更)**: **不做比分栏左右翻转** —— 界面直接标注 **"红队" / "蓝队"** 文字/标识识别双方(客户端用 `Get Player Controller` → `Cast to ShooterPlayerController` → `GetTeam()`:0=RED, 1=BLUE 判断己方)。
- **换弹提示(需求)**: HUD 在换弹期间显示"换弹中"提示(接 `OnReloadStateChanged` 事件)。
- **按钮接线**(蓝图标准流程,非 C++ 自动绑定):
  - 登录菜单"开始游戏"按钮 → 连接 DS(默认本机,或输入框指定的 IP:端口);
  - 主菜单按钮 → `Get Player Controller` → `Cast to ShooterPlayerController` → **`ServerStartMatch`**
  - 结算返回按钮 → 同上 → **`ServerReturnToMainMenu`**

## 10. 关键配置项(BP_TDMGameMode 类默认值)

| 配置 | 默认值 | 说明 |
|---|---|---|
| `KillTarget` | 30 | 获胜所需击杀数 |
| `TeamSize` | 4 | 每队目标人数(人类+AI) |
| `PrepareTime` | 30.0s | 准备阶段倒计时(**需求项**;首位玩家进场后开始,期间禁伤、不生成 AI) |
| `SpawnProtectionTime` | 3.0s | 出生/重生无敌时长(**需求项**) |
| `RespawnTime` | 2.0s | 玩家死亡后的复活倒计时(**需求项**) |
| `BotRespawnDelay` | 3.0s | AI 死亡到补位延迟 |
| `RedSpawnLocations` / `BlueSpawnLocations` | 内置坐标 | 各队 4 个出生点(绕开关卡流送) |
| `ReloadTime` | 0.5s(手枪)/0.8s(步枪)/1.0s(爆弹枪) | 换弹耗时,蓝图可覆盖 |
| `TeamSpawnTags` | `[RED, BLUE]` | 出生点标签(**回退方案**,主方案为坐标配置) |
| `LoadoutWeapons` | 空 | 配枪列表(当前固定默认手枪,不使用) |
| `MainMenuClass` / `EndScreenClass` | BP_TDMMainMenu / BP_TDMEndScreen | 菜单/结算 UI 类 |
| `BotClass` | BP_ShooterNPC | AI 单位类 |

## 11. 当前已知限制(不影响核心流程)

1. **AI 动画不复制**: 客户端看到的 bot 移动动画可能不完整(死亡 ragdoll 已同步)。
2. **换弹无动画/音效**: 换弹仅有时间与状态表现,无换弹动画/音效资源(未配置)。
3. **第三人称武器朝向**: 第三人称视角下枪口随角色转向,不单独跟随俯仰。

## 12. 冲突处理原则

1. 本文档为效果基准;实现与文档不符 → **以文档为准**。
2. 任何**需求变更/冲突** → 先提出、经确认后**更新本文档并升版本**,再改实现。
3. 实现层面的取舍(性能/网络/表现)不影响文档效果时,可自行决策,但需在文档"已知限制"中登记。
